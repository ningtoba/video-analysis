fastapi>=0.138.0
uvicorn[standard]>=0.49.0
gradio>=6.19.0
faster-whisper>=1.2.0
opencv-python>=4.13.0
pillow>=12.0.0
chromadb>=1.5.0
sentence-transformers>=5.6.0
transformers>=5.12.0
torch>=2.12.0
torchvision>=0.27.0
numpy>=2.5.0
langchain-text-splitters>=1.1.0

# BGE-VL / BGE embedding models
einops>=0.8.0

# Structured logging
structlog>=26.0.0

# MCP server (Model Context Protocol for agentic tool access)
mcp>=1.28.0

# Prometheus metrics / production monitoring
prometheus-client>=0.25.0

# Scene understanding
open-clip-torch>=2.30.0

# YouTube/URL video import
yt-dlp>=2026.6.0

# Scene detection
scenedetect>=0.7.0

# Optional — ColBERTv2 late-interaction re-ranking
# ragatouille>=1.0.0

# Optional — video decoding for SmolVLM2
# decord>=0.6.0

# Optional — for higher accuracy:
# ultralytics>=8.4.0  # YOLO object detection
# paddlepaddle-gpu>=3.0.0  # PaddleOCR (v3.7.0+)
# paddleocr>=3.7.0         # OCR text extraction (v3.7.0 — PP-OCRv6, +4.6% det, +5.1% rec over v5)
# pyannote.audio>=4.0.0    # Speaker diarization

# Optional — benchmark / test infrastructure:
# pytest-timeout>=2.3.0
# pytest-benchmark>=4.0.0

